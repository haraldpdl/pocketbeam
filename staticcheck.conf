# ST1005 (error strings must be lowercase, no trailing period) is off on
# purpose: connection.go and webdav.go return sentences that the device
# shows verbatim to the user.
checks = ["inherit", "-ST1005"]
